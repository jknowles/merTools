# Clean formula

a function to modify the formula for a merMod object to create a model
matrix with all predictor terms in both the group level and fixed effect
level

## Usage

``` r
formulaBuild(model)
```

## Arguments

- model:

  a merMod object from lme4

## Value

a formula object
