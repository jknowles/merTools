# Package index

## All functions

- [`FEsim()`](FEsim.md) :

  Simulate fixed effects from merMod `FEsim` simulates fixed effects
  from merMod object posterior distributions

- [`ICC()`](ICC.md) : Calculate the intraclass correlation using mixed
  effect models

- [`REcorrExtract()`](REcorrExtract.md) : Extract the correlations
  between the slopes and the intercepts from a model

- [`REextract()`](REextract.md) : Extracts random effects

- [`REimpact()`](REimpact.md) : Calculate the weighted mean of fitted
  values for various levels of random effect terms.

- [`REmargins()`](REmargins.md) : Calculate the predicted value for each
  observation across the distribution of the random effect terms.

- [`REquantile()`](REquantile.md) : Identify group level associated with
  RE quantile

- [`REsdExtract()`](REsdExtract.md) : Extract the standard deviation of
  the random effects from a merMod object

- [`REsim()`](REsim.md) :

  Simulate random effects from merMod `REsim` simulates random effects
  from merMod object posterior distributions

- [`RMSE.merMod()`](RMSE.merMod.md) : Estimate the Root Mean Squared
  Error (RMSE) for a lmerMod

- [`VarCorr(`*`<merModList>`*`)`](VarCorr.merModList.md) : Extract the
  variances and correlations for random effects from a merMod list

- [`averageObs()`](averageObs.md) : Find the average observation for a
  merMod object

- [`collapseFrame()`](collapseFrame.md) : Collapse a dataframe to a
  single average row

- [`draw()`](draw.md) : Draw a single observation out of an object
  matching some criteria

- [`expectedRank()`](expectedRank.md) : Calculate the expected rank of
  random coefficients that account for uncertainty.

- [`famlink()`](famlink.md) : Find link function family

- [`fastdisp()`](fastdisp.md) : fastdisp: faster display of model
  summaries

- [`fetch.merMod.msgs()`](fetch.merMod.msgs.md) : Extract all warning
  msgs from a merMod object

- [`findFormFuns()`](findFormFuns.md) :

  `findFormFuns` used by
  [averageObs](https://rdrr.io/pkg/merTools/man/averageObs.html) to
  calculate proper averages

- [`fixef(`*`<merModList>`*`)`](fixef.merModList.md) : Extract
  fixed-effects estimates for a merModList

- [`hasWeights()`](hasWeights.md) : Identify if a merMod has weights

- [`hsb`](hsb.md) : A subset of data from the 1982 High School and
  Beyond survey used as examples for HLM software

- [`merModList-class`](merModList-class.md)
  [`merModList`](merModList-class.md) : merModList S3 Class

- [`lmerModList()`](merModList.md) [`blmerModList()`](merModList.md)
  [`glmerModList()`](merModList.md) [`bglmerModList()`](merModList.md) :
  Apply a multilevel model to a list of data frames

- [`modelFixedEff()`](modelFixedEff.md) : Extract averaged fixed effect
  parameters across a list of merMod objects

- [`modelInfo()`](modelInfo.md) : Extract model information from a
  merMod

- [`modelRandEffStats()`](modelRandEffStats.md) : Extract data.frame of
  random effect statistics from merMod List

- [`plotFEsim()`](plotFEsim.md) : Plot the results of a simulation of
  the fixed effects

- [`plotREsim()`](plotREsim.md) : Plot the results of a simulation of
  the random effects

- [`plot_sim_error_chks()`](plot_sim_error_chks.md) : Extract all
  warning msgs from a merMod object

- [`predictInterval()`](predictInterval.md) : Predict from merMod
  objects with a prediction interval

- [`print(`*`<merModList>`*`)`](print.merModList.md) : Summarize a
  merMod list

- [`print(`*`<summary.merModList>`*`)`](print.summary.merModList.md) :
  Print the summary of a merMod list

- [`randomObs()`](randomObs.md) : Select a random observation from model
  data

- [`ranef(`*`<merModList>`*`)`](ranef.merModList.md) : Extract
  random-effects estimates for a merModList

- [`sanitizeNames()`](sanitizeNames.md) : Clean up variable names in
  data frames

- [`setup_parallel()`](setup_parallel.md) : Set up parallel environment

- [`shinyMer()`](shinyMer.md) : Launch a shiny app to explore your
  merMod interactively

- [`shuffle()`](shuffle.md) : Randomly reorder a dataframe

- [`simulate_random_effects()`](simulate_random_effects.md) : Simulate
  random‑effect contributions for all grouping factors

- [`stripAttributes()`](stripAttributes.md) : Remove attributes from a
  data.frame

- [`subBoot()`](subBoot.md) : Bootstrap a subset of an lme4 model

- [`subsetList()`](subsetList.md) : Subset a data.frame using a list of
  conditions

- [`summary(`*`<merModList>`*`)`](summary.merModList.md) : Print the
  results of a merMod list

- [`superFactor()`](superFactor.md) : Create a factor with unobserved
  levels

- [`thetaExtract()`](thetaExtract.md) : Extract theta parameters from a
  merMod model

- [`wiggle()`](wiggle.md) : Assign an observation to different values
